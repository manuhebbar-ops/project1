git remote -v   // command is used to display the URLs that Git uses to fetch from and push to for each remote repository //  
git remote add new_origin_name https://github.com/manuhebbar-ops/abc_project.git
git config --global user.name "Your Name"
:git config --global user.email "Your Name"
